const { corsAllowances } = require('./corsAllowances.js')
const { cookieSettings } = require('./cookiesSettings.js');
const { errorHandler } = require('./errorHandler.js');

module.exports = {
    corsAllowances,
    cookieSettings,
    errorHandler
}
