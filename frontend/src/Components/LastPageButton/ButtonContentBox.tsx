import styled from "styled-components";

//Container responsible for spacing and the react icon and the text content of the last page btn
const ButtonContentBox = styled.div`
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--default-spacing);
`

export default ButtonContentBox